housing_market_data:
  outputs:
    dev:
      dataset: '{{env_var("DATASET_NAME")}}'
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: '{{env_var("GCS_CREDENTIALS")}}'
      location: '{{env_var("REGION", "US")}}'
      method: service-account
      priority: interactive
      project: '{{env_var("PROJECT_ID")}}'
      threads: 4
      type: bigquery
  target: dev
